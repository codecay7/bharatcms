import controllers from './controllers';
import services from './services';
import routes from './routes';

export default {
  register() {},
  bootstrap() {},
  controllers,
  services,
  routes,
};
